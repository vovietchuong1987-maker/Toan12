# Math12 Hub v40.34.0 — UX Consistency & Teacher Workflow

## Mục tiêu
v40.34 dừng bổ sung tính năng trang trí và tập trung xử lý các điểm nợ UX còn lại sau v40.33:

1. Thống nhất Alert / Confirm / Prompt.
2. Tạo một luồng công việc rõ ràng cho giáo viên.
3. Chuẩn hóa trạng thái Loading / Empty / Error.
4. Giữ nguyên nền dữ liệu, Firestore Rules và các module học sinh từ v40.33.

## 1. Hệ thống Dialog thống nhất
- Toàn bộ `confirm()` và `prompt()` của mã ứng dụng đã được chuyển sang `Math12UI.confirm()` / `Math12UI.prompt()` bất đồng bộ.
- `alert()` cũ được bridge sang Toast hoặc Dialog mới, không còn bật hộp thoại native của trình duyệt trong luồng ứng dụng thông thường.
- Dialog mới hỗ trợ:
  - Esc để hủy/đóng.
  - Enter để xác nhận trường nhập.
  - Focus trap và khôi phục focus sau khi đóng.
  - Cảnh báo thao tác nguy hiểm bằng nút/biểu tượng phù hợp.
  - Bottom-sheet trên màn hình nhỏ.
- Các thao tác phá hủy dữ liệu vẫn yêu cầu xác nhận rõ ràng; thao tác xác nhận bằng token như `XOA`, `THAYTHE` vẫn giữ nguyên cơ chế.

## 2. Quy trình giáo viên
Trang chủ giáo viên có thêm khối `QUY TRÌNH GIÁO VIÊN`.

### Bản Classroom đầy đủ
Nếu gói có trang lớp học, hệ thống hiển thị:
`Ngân hàng → Tạo đề → Giao lớp → Theo dõi → Phân tích → Can thiệp`.

### Bản GitHub Lite hiện tại
Do gói v40.33 Lite đã chủ động loại `page-online` và `page-teacher`, workflow tự đổi sang:
`Ngân hàng → Tạo đề → Trợ lý AI → Xem như học sinh → Luyện đề → Thông báo`.

Như vậy không tạo nút chết hoặc dẫn tới trang đã bị loại khỏi gói Lite.

- Ghi nhớ tối đa 4 khu vực giáo viên truy cập gần nhất trên thiết bị.
- Hiển thị các lối tắt `Gần đây` ngay trên workflow.
- Không phát sinh Firestore Read/Write mới.

## 3. Loading / Empty / Error
Các vùng trạng thái cũ như `.online-empty`, `.teacher-live-empty`, `.analytics-empty` được nhận diện và trang trí thống nhất:
- Loading: spinner + `role=status`.
- Empty: trạng thái trống trung tính.
- Error: cảnh báo rõ + `role=alert`.
- Success: dấu xác nhận.

Có API `Math12UI.state(...)` để các module sau này dùng chung thay vì tự tạo HTML trạng thái riêng.

## 4. Điều hướng
- Thêm progress bar mỏng ở đỉnh màn hình khi đổi trang.
- Ghi nhớ trang giáo viên gần đây.
- `?safe=1` vẫn giữ UI dialog cần thiết cho compatibility, nhưng tắt Teacher Workflow và wrapper điều hướng v40.34.

## 5. Tương thích
Không thay đổi:
- Firestore schema.
- `firestore.rules`.
- Dữ liệu học sinh.
- Gamification.
- Student Home v40.32.
- Student Learning Flow v40.33.

## 6. File mới
- `assets/js/ux-consistency-v40.34.0.js`
- `assets/css/ux-consistency-v40.34.0.css`
- `V40.34.0-UX-CONSISTENCY-TEACHER-WORKFLOW.md`
- `DEPLOY-V40.34.md`
