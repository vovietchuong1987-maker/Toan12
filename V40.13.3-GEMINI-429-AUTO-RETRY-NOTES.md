# Math12 Hub V40.13.3 — Gemini 429 Auto-Retry

Nâng trực tiếp từ V40.13.2, không thay đổi ngân hàng câu hỏi hay các module cũ.

## Nâng cấp chính

- Bắt chính xác HTTP 429 / quota/rate-limit từ Gemini.
- Đọc `Retry-After`, `RetryInfo.retryDelay` hoặc câu `Please retry in ...s`.
- Mặc định tự đếm ngược và chạy lại **đúng batch đang dở**, không tăng `nextOrdinal` trước khi batch thành công.
- Tối đa 6 lần retry tự động cho một batch; thời gian chờ tự động tối đa 180 giây/lần. Nếu server yêu cầu lâu hơn, pipeline dừng an toàn tại checkpoint.
- Có lựa chọn `Tự chờ & tiếp tục` hoặc `Dừng tại checkpoint`.
- Hiển thị số request trong 60 giây gần nhất trên trình duyệt, giới hạn server gần nhất (nếu Gemini trả về) và số lần 429. Bộ đếm là ước tính cục bộ.
- Nút Dừng hoạt động cả khi đang chờ 429.
- Tạo 1 batch cũng có retry 429 tối đa 3 lần.
- Chẩn đoán Gemini được rút từ 4 probe xuống một request thông minh trong trường hợp bình thường để tiết kiệm quota.
- Cache/Lazy-load/Service Worker nâng lên V40.13.3.

## Nguyên tắc an toàn

- Không tự publish câu hỏi.
- Không bỏ checkpoint khi 429.
- Không ghi API key vào Firestore/state/backup.
- Nếu cùng API key được dùng trên thiết bị/app khác, bộ đếm request cục bộ có thể thấp hơn quota thực tế của Google.
