# Math12 Hub v40.13.6 — Smart Figure Mapping & Review Center

## Mục tiêu
Hoàn thiện pipeline nhập câu có hình sau v40.13.5: ghép hình Word chính xác hơn và thêm một trung tâm kiểm duyệt hình trước khi đưa câu vào ngân hàng.

## Nâng cấp chính
- **Smart Word Figure Mapping**:
  - đọc `word/document.xml` và `document.xml.rels` của `.docx`;
  - theo dõi số thứ tự `Câu n` trong luồng tài liệu;
  - ánh xạ `r:embed` của hình tới đúng file trong `word/media/`;
  - gắn `sourceOrdinalHint`, `nearText`, `relationshipId` cho ảnh;
  - nếu AI trả `sourceOrdinal` trùng với vị trí hình Word, ưu tiên ghép chính xác theo ordinal với confidence 95%.
- **Figure Review Center**:
  - nút `🖼 Kiểm duyệt hình` trong Hàng chờ AI;
  - lọc nhanh: Tất cả / Thiếu hình / Chưa xác nhận / Đã xác nhận;
  - xem thumbnail, số thứ tự nguồn, độ tin cậy ghép;
  - đổi/gán hình bằng Figure Picker của v40.13.5;
  - giáo viên bấm `✓ Đúng hình` để xác nhận thủ công.
- **QC hình bắt buộc**:
  - câu có nhắc tới hình nhưng chưa gắn hình sẽ bị chặn khỏi nhóm `Đạt QC`;
  - hình tự gán nhưng chưa được giáo viên xác nhận cũng chưa được tính là `Đạt QC`;
  - hình được giáo viên gán thủ công hoặc xác nhận sẽ được phép đi tiếp nếu các kiểm tra khác đều đạt.
- **Ghép lại theo Word**:
  - nút `↻ Ghép lại theo Word` chạy lại ánh xạ ordinal cho các draft hiện tại;
  - không ghi đè hình đã được giáo viên gán/xác nhận thủ công.

## Kế thừa nguyên vẹn
- Gemini 400 compatibility fallback v40.13.1.
- Self Diagnostic v40.13.2.
- 429 auto-retry/checkpoint v40.13.3.
- Figure Attach v40.13.4.
- Figure Picker + figureMode=image editor support v40.13.5.

## Lưu ý PDF
PDF vẫn được gửi trực tiếp cho Gemini và câu có thể được trích xuất bình thường. v40.13.6 tập trung ánh xạ hình tự động chính xác cho Word; với PDF nhiều hình/scan, giáo viên dùng Trung tâm kiểm duyệt hình + Gán hình/Đổi hình để xác nhận trước khi import.
