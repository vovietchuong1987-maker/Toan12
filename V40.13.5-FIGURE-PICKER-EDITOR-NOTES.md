# Math12 Hub v40.13.5 — Figure Picker & Image Editor Support

## Mục tiêu
Hoàn thiện bước sau v40.13.4: cho phép giáo viên gán/chỉnh lại hình cho từng bản nháp AI và chỉnh sửa/lưu ổn định các câu có hình ảnh gốc đính kèm.

## Nâng cấp chính
- Thêm nút **Gán hình / Đổi hình / Bỏ hình** ngay trên từng draft card trong Hàng chờ AI.
- Mở modal chọn ảnh từ:
  - bộ ảnh nguồn đã tách/lưu từ lần import gần nhất
  - hoặc tải ảnh thủ công từ máy
- Trình soạn câu hỏi hỗ trợ mode mới: **Hình ảnh gốc đính kèm** (`figureMode=image`).
- Khi mở/sửa câu có hình ảnh gốc, ảnh vẫn được giữ lại và xem được trong preview.
- `saveQuestionEditor()` đã được vá để lưu được câu có hình ảnh gốc, không còn bị lỗi “đã chọn chế độ hình nhưng chưa nhập mã hình”.

## Phạm vi
- Tập trung vào trải nghiệm kiểm tra thủ công sau AI: thầy/cô xem draft nào gắn sai hình thì đổi lại ngay.
- Bản này chưa thay đổi phần OCR lõi của PDF scan; nó nâng phần **kiểm duyệt và gán hình** lên mức thực chiến hơn.

## Gợi ý dùng
1. Nhập bằng ảnh hoặc Word có hình.
2. AI sinh draft vào Hàng chờ.
3. Nếu câu có hình, bấm **Gán hình/Đổi hình** để kiểm tra lại.
4. Khi cần chỉnh nội dung, bấm **Mở trình soạn** — ảnh vẫn còn.
5. Duyệt từng câu hoặc import hàng loạt các câu đạt QC.
