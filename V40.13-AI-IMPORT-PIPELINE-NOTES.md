# Math12 Hub — AI Import Pipeline

Build: `40.13-ai-import-pipeline`

Nâng trực tiếp từ bản `40.12-compact-smart-navigation`, giữ nguyên ngân hàng hợp nhất V40.11, ID câu cố định, ID6, Figure/TikZ/BBT, Exam Pro, avatar, shop, room và navigation hiện có.

## Mục tiêu

Giảm thời gian nhập ngân hàng câu hỏi bằng quy trình:

`Word / PDF / ảnh / LaTeX → Gemini → chuẩn hóa cấu trúc → round-trip LaTeX → QC → kiểm tra trùng → Hàng chờ AI → giáo viên duyệt → Question Bank`.

Gemini không tự publish câu hỏi.

## Điểm mới

- Nhận `.docx`, `.pdf`, `.png/.jpg/.webp`, `.tex/.txt`.
- `.docx` được giải nén ngay trong trình duyệt; lấy văn bản và tối đa 4 ảnh nhúng phù hợp để gửi Gemini.
- Chế độ nhập hàng loạt theo batch 5 / 10 / 15 câu.
- Mục tiêu tối đa 30 / 60 / 120 / 250 câu.
- Checkpoint lưu cục bộ; nếu gặp quota/rate limit có thể tiếp tục từ batch kế tiếp thay vì làm lại từ đầu.
- Hàng chờ AI tăng lên tối đa 250 câu, có giới hạn dung lượng an toàn của localStorage.
- Mỗi câu có `sourceFingerprint`, `sourceOrdinal`, `sourceLabel`, `batchNo` để truy vết nguồn và tránh lặp khi resume.
- Validator V40.13 kết hợp:
  - QC cấu trúc câu hiện có;
  - kiểm tra metadata / mã kiến thức;
  - kiểm tra trùng với Question Bank;
  - kiểm tra trùng trong chính Hàng chờ AI;
  - `validateQuestionLatexItem`;
  - xuất `v29QuestionToLatex` rồi parse ngược qua `parseBulkLatexSource` để kiểm tra round-trip.
- Chỉ câu đạt cổng QC mới có thể được chọn để đưa hàng loạt vào Question Bank.
- Bulk import chỉ đưa vào trạng thái `draft`; không tự đánh dấu đã duyệt chuyên môn.
- Khi câu được đưa vào Question Bank, `QuestionIdV40` cấp ID cố định `Qxxxxxx`.
- Có xem/copy LaTeX từng bản nháp và xuất `.tex` toàn bộ hoặc các câu đang chọn.
- Sửa cache-busting của lazy-loaded AI và đăng ký Service Worker đúng `sw.js` để bản mới cập nhật ổn định trên GitHub Pages.

## Nguyên tắc an toàn dữ liệu

- API key Gemini vẫn chỉ nằm trong trình duyệt theo lựa chọn của giáo viên.
- Tệp gốc không được lưu vào Firestore.
- Pipeline checkpoint chỉ lưu metadata tiến trình, không lưu tệp gốc.
- Câu AI luôn là bản nháp cho đến khi giáo viên quyết định.

## Triển khai

Upload toàn bộ nội dung gói GitHub Pages. Sau khi thay bản, tải lại mạnh (`Ctrl + F5`) một lần để trình duyệt nhận cache revision mới.
