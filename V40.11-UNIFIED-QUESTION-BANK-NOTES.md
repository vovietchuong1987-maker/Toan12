# Math12 Hub V40.11 — Unified Question Bank

## Mục tiêu
Loại bỏ tình trạng ngân hàng quản trị và ngân hàng học sinh dùng hai bộ dữ liệu khác nhau (430 câu so với static 371 câu).

## Nguồn chuẩn của bản phát hành
- File thầy cung cấp: `math12-question-bank-legacy-v37.1-20260903-2043.json`.
- 430 câu.
- 430/430 `approved`.
- 340 MCQ, 29 Đúng/Sai 4 ý, 61 Trả lời ngắn.
- 430 `questionId` duy nhất; high-water Q000498.
- 430/430 metadataStatusV36 = `complete`.

## Thay đổi V40.11
1. Thay static 371 câu bằng `assets/data/question-bank-canonical-v40.11.js` chứa đúng 430 câu từ backup hiện tại.
2. `Math12Content` trở thành contract nguồn dữ liệu thống nhất:
   - Giáo viên/Admin: `state.questionBank` hiện tại là nguồn tác giả.
   - Học sinh: snapshot công khai V40.11 nếu Firestore sẵn sàng.
   - Offline/không đăng nhập/chưa cập nhật Rules: bundle 430 câu là fallback.
3. Dashboard “Tình trạng ngân hàng học tập” đọc cùng `Math12Content.readiness()` với phần tự luyện.
4. Bổ sung `unified-bank-v40.11.js`:
   - Tự seed ngân hàng chuẩn cho tài khoản giáo viên mới nếu chưa có bank.
   - Tự publish Approved/Reviewed thành các chunk Firestore khi bank thay đổi.
   - Học sinh tự tải snapshot mới nhất khi đăng nhập.
   - Có hash để không ghi Firestore lại khi nội dung không đổi.
5. Bổ sung Firestore Rules cho `publicQuestionBankV4011`.
6. Service Worker đổi cache V40.11 và precache đúng file canonical mới.

## Kỳ vọng với dữ liệu hiện tại
Dashboard phải hiển thị:
- Tổng/đủ điều kiện: 430/430.
- Chương 1: 430.
- MCQ: 340.
- Đ/S: 29.
- Ngắn: 61.
- Bị chặn: 0.

## Lưu ý triển khai Firestore
Bản GitHub vẫn chạy đủ 430 câu ngay cả khi chưa cập nhật Rules nhờ bundle offline. Để chức năng tự publish/live-sync ngân hàng dùng được giữa các thiết bị, cập nhật `firestore.rules.template` lên Firebase Rules một lần.
