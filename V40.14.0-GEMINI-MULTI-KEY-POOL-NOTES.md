# Math12 Hub v40.14.0 — Gemini Multi-Key Pool

## Mục tiêu
Cho phép giáo viên cấu hình nhiều Gemini API key và tự chuyển key khi một key/project không còn khả dụng, trong khi vẫn giữ Smart RPM Scheduler, checkpoint và toàn bộ pipeline AI hiện có.

## Cơ chế chính
- Quản lý tối đa 20 API key trong trình duyệt.
- Secret không đưa vào `state`, Firestore, backup hoặc audit log.
- Mỗi key có:
  - tên API
  - nhóm Project
  - chế độ lưu phiên/thiết bị
  - trạng thái sẵn sàng / đang dùng / cooldown / lỗi xác thực / tắt
- Tự migrate key đơn cũ thành `API hiện tại • Project 1`.

## Failover
### 429 / quota / rate limit
- Cooldown toàn bộ key có cùng `Nhóm Project`.
- Chỉ tự chuyển sang một key thuộc **Project group khác** đang sẵn sàng.
- Nếu không có project khác, trả lỗi 429 cho lớp retry/checkpoint cũ để tự chờ rồi tiếp tục.

### 401 / 403 / authentication
- Chặn riêng key lỗi xác thực.
- Tự thử key sẵn sàng tiếp theo; có thể cùng Project vì lỗi xác thực là lỗi credential, không phải quota chung.

### Các lỗi khác
- 400 / invalid argument / lỗi nội dung không kích hoạt đổi key.
- Giữ nguyên cơ chế fallback schema của Gemini transport hiện tại.

## Smart RPM theo Project
- Scheduler v40.13.9 được tách lịch sử theo `Nhóm Project`.
- Project 1 và Project 2 có cửa sổ RPM riêng.
- Các key cùng Project dùng chung cửa sổ scheduler.
- 429 học limit riêng cho project đang hoạt động.

## Giao diện
Trong Cài đặt Gemini có khối `GEMINI MULTI-KEY POOL`:
- Thêm API
- đặt tên
- đặt Nhóm Project
- lưu theo phiên / thiết bị
- Dùng / Test / Sửa / Bật-Tắt / Xóa từng key
- Test tất cả key
- Chuyển key ngay

## Lưu ý quan trọng
Các API key thuộc cùng Google Cloud / Gemini Project phải được nhập **cùng một Nhóm Project**. Việc này giúp Math12 Hub không luân phiên vô ích giữa nhiều key vốn dùng chung một quota.
