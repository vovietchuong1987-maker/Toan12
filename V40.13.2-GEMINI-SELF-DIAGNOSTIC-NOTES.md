# Math12 Hub V40.13.2 — Gemini Self Diagnostic

Build: `40.13.2-gemini-self-diagnostic`

- Thêm nút **Tự chẩn đoán Gemini** theo 4 tầng: request text tối giản, JSON mode, structured JSON Schema, Vision với ảnh đang chọn.
- Không hiển thị API key trong kết quả chẩn đoán. Nhận biết key dạng `AQ.` để giải thích riêng lỗi xác thực.
- Structured output ưu tiên `responseJsonSchema` theo API hiện hành; vẫn có fallback `responseSchema` legacy với enum type viết hoa cho raw REST.
- Nếu schema/ThinkingConfig lỗi 400, tự rơi xuống JSON mode rồi payload tối giản không `systemInstruction`/`generationConfig`.
- Phân biệt rõ 400 INVALID_ARGUMENT, 401 UNAUTHENTICATED/ACCESS_TOKEN_TYPE_UNSUPPORTED, 429 quota, lỗi Vision và lỗi mạng/CORS.
- Giữ nguyên AI Import Pipeline, checkpoint, Question Bank, ID6, Avatar, Firestore và các module cũ.
