# Math12 Hub V40.10 — BBT/BXD Source Auto-Detect

## Mục tiêu
- Tự nhận diện `tkz-tab` từ `figureLatex` dù metadata cũ đang là `tikz` hoặc `tkz`.
- Ưu tiên Native BBT/BXD Renderer cho nguồn `tkz-tab`.
- Giữ quy tắc V40.6: nguồn chỉ có `x` và `f'(x)` thì chỉ dựng 2 dòng, tuyệt đối không tự sinh `f(x)`.
- Nhận dạng an toàn một số bảng biến thiên TikZ tự kẻ kiểu cũ và chuyển runtime sang cấu trúc `tkz-tab` tương đương.
- Không sửa nội dung toán, đáp án, ID6, Question ID hay Firestore.

## Trường hợp xác nhận
Bảng TikZ cũ của Q000288 với các mốc `0, 200, +∞`, dấu `+,0,-` và cực đại `100` được nhận dạng thành:

```latex
\begin{tikzpicture}
\tkzTabInit{$x$/1,$f'(x)$/1,$f(x)$/2}{$0$,$200$,$+\infty$}
\tkzTabLine{,+,z,-,}
\tkzTabVar{-/,+/$100$,-/}
\end{tikzpicture}
```

## Chính sách an toàn
Các bảng TikZ cũ có cấu trúc phức tạp như gián đoạn/tiệm cận với vạch kép nhưng chưa đủ dữ liệu để chuyển chắc chắn sẽ tiếp tục đi qua pipeline TikZ cũ; V40.10 không đoán cấu trúc toán học.
