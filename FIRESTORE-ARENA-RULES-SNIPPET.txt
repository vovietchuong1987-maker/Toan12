// Merge this snippet INSIDE your existing service cloud.firestore { match /databases/{database}/documents { ... } }
// Review alongside your current rules before publishing.

match /arenaRooms/{roomId} {
  allow read, create: if request.auth != null;
  allow update, delete: if request.auth != null && resource.data.ownerUid == request.auth.uid;

  match /players/{uid} {
    allow read: if request.auth != null;
    allow create, update, delete: if request.auth != null && request.auth.uid == uid;
  }
}

// Note: v40.25 scores are client-reported from a completed Math12 Hub attempt.
// For high-stakes/official competition, validate submissions with a trusted backend/Cloud Function.
