# Math12 Hub v40.13.4 — Figure Extraction & Figure Attach

## Mục tiêu
Bổ sung khả năng giữ và gắn hình vẽ/hình gốc của câu hỏi trong AI Import Pipeline, đặc biệt cho nguồn ảnh hoặc Word có hình nhúng.

## Nâng cấp chính
- Tự phân tích nguồn hiện tại để lấy **ảnh gốc / ảnh nhúng** trước khi chạy AI.
- Khi bản nháp chứa tín hiệu “có hình”, hệ thống tự gắn ảnh nguồn vào câu với `figureMode: image`.
- Hàng chờ AI hiển thị **thumbnail hình gốc** ngay trên từng draft card.
- Preview, exam, ngân hàng và modal xem câu đã hiển thị được `figureMode=image`.
- Khi export LaTeX cho câu có hình ảnh gốc, hệ thống chèn **metadata comment** để nhắc cần thay bằng `\includegraphics` hoặc vẽ lại TikZ khi dùng LaTeX độc lập.

## Phạm vi hiện tại
- Hỗ trợ tốt nhất cho:
  - file ảnh (PNG/JPG/WEBP): gắn ảnh gốc vào câu có hình
  - Word `.docx`: lấy các ảnh nhúng trong `word/media/` và gắn theo heuristic
- PDF: vẫn đọc tốt cho Gemini, nhưng **chưa tách hình riêng ở phía trình duyệt** trong bản này; có nhắc kiểm tra thủ công.

## Cách gắn hình
- Nếu nguồn có 1 hình: gắn hình đó cho các câu có dấu hiệu liên quan tới hình.
- Nếu nguồn có nhiều hình: ưu tiên ghép theo `sourceOrdinal` hoặc theo thứ tự xuất hiện.
- Các câu được gắn hình được thêm tag `figure-source-image`.

## Lưu ý
- Đây là bước **giữ hình gốc đi kèm câu**, không phải OCR hoàn hảo từng vùng hình trong PDF scan.
- Với đề có nhiều hình gần nhau hoặc scan mờ, thầy/cô vẫn nên xem lại trong Hàng chờ AI trước khi import hàng loạt.
