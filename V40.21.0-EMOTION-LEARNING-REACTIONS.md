# Math12 Hub v40.21.0 — Emotion & Learning Reactions

- Character Platform phản ứng theo kết quả học thật.
- Điểm cao / chuỗi đúng / lên level tạo phản ứng vui, tự hào hoặc ăn mừng.
- Chuỗi sai tạo trạng thái tập trung hoặc suy nghĩ, không dùng ngôn ngữ gây áp lực.
- Animation chỉ dùng CSS + asset hiện tại; không khôi phục renderer SVG nhân vật cũ.
- Không thay đổi điểm, đáp án, độ khó hay phần thưởng của hệ thống chấm.
- Có API `M12CharacterReactions.trigger()` và `M12CharacterPlatform.react()` cho các module sau.
