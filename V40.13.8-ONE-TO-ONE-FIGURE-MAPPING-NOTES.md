# Math12 Hub v40.13.8 — One-to-One Figure Mapping Fix

## Lỗi được sửa
Ở các bản v40.13.4–v40.13.7, nếu nguồn chỉ có một ảnh thì fallback cũ có thể gắn cùng ảnh đó cho nhiều câu bị đánh dấu “có khả năng cần hình”. Bộ phát hiện cũ cũng quá rộng vì coi các từ như `Oxyz`, `Oxy`, `trục tọa độ`, `hình hộp` là tín hiệu cần ảnh.

## Nguyên tắc mới
- Một ảnh nguồn chỉ được **tự gắn tối đa cho một câu**.
- Với Word `.docx`, quan hệ ảnh trong `document.xml` + `document.xml.rels` và `sourceOrdinal` là tín hiệu ưu tiên cao nhất.
- Nếu ảnh Word được xác định nằm trong Câu n, ảnh chỉ được gắn cho draft có `sourceOrdinal=n`.
- Nếu ảnh không có ordinal, chỉ tự gắn khi cả **ảnh còn lại = 1** và **câu có tham chiếu hình rõ ràng còn lại = 1**.
- Không tự coi `Oxyz`, `Oxy`, `hình hộp`, `hình chóp`, `trục tọa độ` là tham chiếu ảnh.
- Các cụm mạnh như `như hình vẽ bên`, `quan sát hình`, `theo hình`, `đồ thị ... hình`, `bảng biến thiên`, `biểu đồ` vẫn được nhận diện.

## Migration tự động
Khi mở bản v40.13.8, các gán ảnh **tự động** của v40.13.4/v40.13.6 được rà lại:
- gỡ những gán tự động cũ;
- ghép lại theo one-to-one;
- không đụng vào hình giáo viên đã gán thủ công hoặc đã xác nhận.

## Kiểm duyệt hình
Nút `↻ Dọn & ghép lại 1→1` trong Trung tâm kiểm duyệt hình sẽ chạy lại toàn bộ mapping theo logic mới.

## QC
Câu chỉ bị đánh dấu `Thiếu hình` khi có tham chiếu hình rõ ràng. Câu chỉ có ngữ cảnh hình học/toạ độ nhưng không tham chiếu ảnh sẽ không còn bị chặn QC vì thiếu hình.
