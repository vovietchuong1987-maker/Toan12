# Math12 Hub v40.13.7 — Auto ID6 Classifier

## Mục tiêu
Tự động gắn ID6 cho câu được tạo từ AI Import Pipeline mà vẫn giữ Math12 Hub là nguồn quyết định taxonomy cuối cùng.

## Nguyên tắc
- Gemini **không tạo ID6 tự do**.
- Gemini chỉ chọn một `id6Pattern` trong **91 dạng chính thức** đang có trong `ID6V374`.
- Math12 Hub kiểm tra pattern bằng `formByPattern()` rồi dùng `buildId6(pattern, level)` để sinh ID6 hoàn chỉnh.
- `Qxxxxxx` vẫn là ID duy nhất của câu; ID6 chỉ là mã phân loại.

## Dữ liệu AI mới
Mỗi câu AI có thêm:
- `id6Pattern`: mẫu chính thức, ví dụ `2D1?1-2`
- `id6Confidence`: 0–100
- `id6Reason`: lý do phân loại ngắn

## Ba mức xử lý
- `>= ngưỡng tự động` (mặc định 90%): gắn ID6 tự động và đồng bộ metadata.
- `75% → dưới ngưỡng tự động`: gắn ID6 đề xuất nhưng trạng thái **Cần kiểm tra**; bulk import bị chặn cho tới khi xác nhận.
- `<75%` hoặc pattern không hợp lệ: **Chưa có ID6**; bulk import bị chặn.

Ngưỡng ID6 tự động có thể chọn 85%, 90% hoặc 95% ngay trong AI Import Pipeline.

## Bộ phân loại hai tầng
1. Gemini đề xuất pattern và confidence.
2. Bộ phân loại cục bộ so lại với:
   - ID6/form hiện có
   - alias taxonomy
   - lessonId
   - tên dạng chính thức
   - nội dung câu
3. Nếu Gemini và local classifier mâu thuẫn mạnh, confidence bị hạ xuống mức cần kiểm tra.

## Đồng bộ metadata
Khi ID6 đạt mức Auto hoặc được giáo viên xác nhận, hệ thống đồng bộ:
- `id6Pattern`
- `id6`
- `formId`
- `form` / `id6Title`
- `lessonId`
- `knowledgeCode` khi xác định được
- metadata hierarchy Chương/Bài/Dạng

Với Chương 1, hệ thống tiếp tục dùng lớp `v3821Taxonomy.canonicalizeQuestion()` hiện có.

## UI mới
Trong Hàng chờ AI:
- badge ID6 cho từng draft
- nút **ID6 / Gán ID6**
- nút **🏷 Kiểm duyệt ID6**

Trung tâm kiểm duyệt ID6 có các nhóm:
- Tất cả
- ID6 tự động
- Cần kiểm tra
- Chưa có ID6
- Đã xác nhận

Có thể:
- đổi ID6 thủ công từ danh mục 91 dạng
- xác nhận từng câu
- xác nhận hàng loạt các câu đang ở trạng thái ID6 tự động
- phân loại lại toàn bộ khi thay đổi ngưỡng Auto

## QC / Bulk import
Một draft chỉ giữ trạng thái `safe` nếu ID6 ở một trong hai trạng thái:
- `auto`
- `confirmed`

Các draft `review` hoặc `missing` không được chọn vào bulk import tự động.

## Regression tests đã chạy
- `2D1?1-2` + level `TH` → `2D1H1-2`
- Gemini + local classifier đồng thuận → Auto
- Gemini/local mâu thuẫn mạnh → Review
- Gemini trả pattern ngoài taxonomy → bị từ chối; không tạo ID6 giả
- schema Gemini chứa đúng 91 pattern ID6 chính thức
